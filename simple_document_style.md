despite the colorful world with various documents styles
if you want to get an intro simple enough about what a program does
there should be a way

here's an example

```
# class
## attrs
> list
- `a` name
- `b` age
- `c` friends

> description
- `a` name
`string` type
`"undefined"` default
- `b` age
...

> code
...

## methods
> list
- `d(name)` find name
- `e()` sort by age
- `f(friend_name)` add a friend

> description
- `d(string name)` find name
`int` return type
`-1` not exist
`n` index of the name

- `e()` sort by age
...

> code
...


```
